# docker-compose.yml - Entrypoint dispatcher pointing to dev or prod compose stack
# Set ENV_FILE and COMPOSE_TARGET in .env to switch environments seamlessly
include:
  - path: ${COMPOSE_TARGET:-docker-compose.dev.yml}
    env_file: ${ENV_FILE:-.env.dev}