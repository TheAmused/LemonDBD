# ==============================================================================
# LemonDBD - Environment Configuration Guide & Example
# ==============================================================================
# LemonDBD uses an environment pointer setup:
#   - .env points to the active environment (.env.dev or .env.prod)
#   - .env.dev contains development settings (tunnel notifier, debug enabled)
#   - .env.prod contains production settings (secure defaults, crowdsec enabled)
#
# To select an environment in .env:
#   ENV_FILE=.env.dev
#   COMPOSE_TARGET=docker-compose.dev.yml
# ==============================================================================

# ------------------------------------------------------------------------------
# 0. Active Environment Selector (used in .env)
# ------------------------------------------------------------------------------
ENV_FILE=.env.dev
COMPOSE_TARGET=docker-compose.dev.yml
COMPOSE_FILE=docker-compose.dev.yml
COMPOSE_ENV_FILES=.env.dev

# ------------------------------------------------------------------------------
# 1. Feature Toggles
# ------------------------------------------------------------------------------
RATELIMIT_ENABLED=false
SCHEDULER_ENABLED=true
INITIAL_SCRAPE_ENABLED=true

# ------------------------------------------------------------------------------
# 2. Backend Server & Runtime Configuration
# ------------------------------------------------------------------------------
FLASK_RUN_HOST=0.0.0.0
FLASK_RUN_PORT=5000
FLASK_DEBUG=true
FLASK_ENV=development

# ------------------------------------------------------------------------------
# 3. Security, Authentication & Session Tokens
# ------------------------------------------------------------------------------
SECRET_KEY=dev-secret-key-dbd-lemon-2026
JWT_SECRET_KEY=dev-secret-key-dbd-lemon-2026
JWT_EXPIRATION_HOURS=24
CORS_ORIGINS=*

# ------------------------------------------------------------------------------
# 4. Outgoing Mail (email verification & password reset)
# ------------------------------------------------------------------------------
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=true
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=
FRONTEND_URL=https://localhost/

# ------------------------------------------------------------------------------
# 5. PostgreSQL Database Configuration
# ------------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=dbd_db
POSTGRES_PORT=5432
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/dbd_db

# ------------------------------------------------------------------------------
# 6. Discord Webhooks & Cloudflare Tunnel
# ------------------------------------------------------------------------------
DISCORD_WEBHOOK_URL=
DISCORD_TUNNEL_WEBHOOK=

# ------------------------------------------------------------------------------
# 7. Frontend Settings & Donation Links
# ------------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=https://localhost
NEXT_PUBLIC_BUY_ME_A_COFFEE_URL=https://buymeacoffee.com/YOUR_USERNAME
NEXT_PUBLIC_KOFI_URL=https://ko-fi.com/YOUR_USERNAME
NEXT_PUBLIC_PATREON_URL=https://patreon.com/YOUR_USERNAME
NEXT_PUBLIC_DONATION_MESSAGE="Fuel the Entity with caffeine to keep LemonDBD servers running 24/7!"

# ------------------------------------------------------------------------------
# 8. Umami Analytics
# ------------------------------------------------------------------------------
UMAMI_APP_SECRET=change-this-to-a-random-secret
UMAMI_PORT=8117
NEXT_PUBLIC_UMAMI_URL=https://localhost:8117
UMAMI_ADMIN_USERNAME=admin
UMAMI_ADMIN_PASSWORD=umami
UMAMI_SITE_NAME=LemonDBD
UMAMI_SITE_DOMAIN=localhost

# ------------------------------------------------------------------------------
# 9. pgAdmin 4
# ------------------------------------------------------------------------------
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@lemondbd.com
PGADMIN_DEFAULT_PASSWORD=admin
NEXT_PUBLIC_PGADMIN_URL=https://localhost:5050